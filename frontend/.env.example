# =============================================================================
# Frontend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# These variables are used at build time and must be prefixed with VITE_
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# VITE_API_BASE_URL: The base URL for your backend API
# 
# For local development (default):
# VITE_API_BASE_URL=/api/v1
#
# When using ngrok (expose backend to internet):
# 1. Start ngrok: docker-compose --profile ngrok up
# 2. Check ngrok dashboard: http://localhost:4040
# 3. Copy your ngrok URL (e.g., https://abc123.ngrok-free.app)
# 4. Set: VITE_API_BASE_URL=https://abc123.ngrok-free.app/api/v1
#
# For production:
# VITE_API_BASE_URL=https://your-domain.com/api/v1
VITE_API_BASE_URL=/api/v1

# -----------------------------------------------------------------------------
# OAuth Configuration
# -----------------------------------------------------------------------------
# VITE_OAUTH_REDIRECT_URL: Where to redirect after Slack OAuth
# This should match your PUBLIC_URL from backend .env
# 
# For local development:
# VITE_OAUTH_REDIRECT_URL=http://localhost:8000/api/oauth/callback
#
# When using ngrok:
# VITE_OAUTH_REDIRECT_URL=https://abc123.ngrok-free.app/api/oauth/callback
VITE_OAUTH_REDIRECT_URL=http://localhost:8000/api/oauth/callback
